<%= form_with(model: mechanism) do |form| %>
  <% if mechanism.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mechanism.errors.count, "error") %> prohibited this mechanism from being saved:</h2>

      <ul>
        <% mechanism.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :mechanism_image %>
    <%= form.file_field :mechanism_image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
