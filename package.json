{
  "name": "myapp",
  "private": "true",
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --outdir=app/assets/builds"
  },
  "dependencies": {
    "bootstrap": "^5.2.3",
    "esbuild": "^0.17.11"
  }
}
